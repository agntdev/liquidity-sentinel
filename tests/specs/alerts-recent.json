[
  {
    "name": "View Recent Alerts — empty state",
    "steps": [
      {
        "send": { "callback": "alerts:recent" },
        "expect": [
          { "method": "answerCallbackQuery" },
          {
            "method": "editMessageText",
            "payload": {
              "text": "No trade alerts yet — alerts will appear here when setups are detected."
            }
          }
        ]
      }
    ]
  }
]
