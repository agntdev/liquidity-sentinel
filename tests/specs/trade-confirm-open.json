[
  {
    "name": "Confirm Trade Opened — empty state",
    "steps": [
      {
        "send": { "callback": "trade:confirm_open" },
        "expect": [
          { "method": "answerCallbackQuery" },
          {
            "method": "editMessageText",
            "payload": {
              "text": "No unconfirmed alerts right now — new setups will appear here."
            }
          }
        ]
      }
    ]
  }
]
