[
  {
    "name": "Report Trade Closed — empty state",
    "steps": [
      {
        "send": { "callback": "trade:report_close" },
        "expect": [
          { "method": "answerCallbackQuery" },
          {
            "method": "editMessageText",
            "payload": {
              "text": "No open trades to close — confirm a trade first."
            }
          }
        ]
      }
    ]
  }
]
